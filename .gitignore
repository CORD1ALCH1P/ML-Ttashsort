.venv/
venv/
__pycache__/
*.pyc

# Исходный датасет и его разбиение — регенерируются скриптом src/prepare_data.py,
# в репозиторий не кладём, чтобы не раздувать его повторно скачиваемыми файлами.
data/raw/
data/split/
data/*.zip

# Обученные веса моделей (models/*.pt) НЕ игнорируем — без них веб-приложение
# не заработает сразу после клонирования, а переобучение занимает ~20 минут.

# Локальные настройки/секреты Streamlit (если появятся)
.streamlit/
secrets.toml

# ОС и редакторы
.DS_Store
Thumbs.db
.vscode/
.idea/
